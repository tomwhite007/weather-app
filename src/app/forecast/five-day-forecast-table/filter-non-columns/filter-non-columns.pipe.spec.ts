import { FilterNonColumnsPipe } from './filter-non-columns.pipe';

describe('FilterNonColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNonColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
